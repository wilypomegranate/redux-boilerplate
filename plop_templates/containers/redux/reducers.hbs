import { fromJS } from 'immutable'


const initialState = fromJS({
})

const {{name}} = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default {{name}}
