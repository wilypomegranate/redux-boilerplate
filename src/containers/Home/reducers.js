import { fromJS } from 'immutable'


const initialState = fromJS({
})

const Home = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default Home
